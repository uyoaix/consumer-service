

  spring:
    application:
      name: consumer-service
    cloud:
      nacos:
        config:
          server-addr: 127.0.0.1:8848
          file-extension: yml
          shared-configs: common-config.yml, consumer-service.yml